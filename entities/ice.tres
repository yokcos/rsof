[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;

uniform sampler2D noise;
uniform sampler2D gradient;


void fragment() {
	vec2 uv;
	uv.x = texture(TEXTURE, UV).r;
	uv.y = texture(noise, UV).r;
	
	uv = 0.5 + 2.0*(uv - 0.5);
	
	COLOR = texture(gradient, uv);
}
"
